{
  "name": "topt-core",
  "version": "0.5.0",
  "description": "Core of TOPT",
  "main": "build/core/index.js",
  "types": "build/core/index.d.ts",
  "scripts": {
    "prepare": "npm run build",
    "test": "jest --noStackTrace --config jestconfig.json --noStackTrace",
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "author": "varunda",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/jquery": "^3.5.4",
    "@types/websocket": "^1.0.1",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.0.5"
  },
  "files": [
    "build/**/*"
  ],
  "dependencies": {
    "axios": "^0.21.0",
    "loglevel": "^1.7.1",
    "loglevel-plugin-prefix": "^0.8.4",
    "websocket": "^1.0.32"
  }
}
